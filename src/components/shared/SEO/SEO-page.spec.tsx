// import React from 'react';
// import { render } from '@testing-library/react';
// import SEO from './SEO';
import { describe, it } from 'vitest';

describe(`SEO`, () => {
  it(`renders SEO component with correct meta tags`, () => {
    // const title = `Test Page`;
    // const description = `This is a test page`;
    // const image = `/test-image.png`;
    // const location = {
    //   href: `https://example.com/test-page`,
    // };
    // const pathname = `/test-page`;
    // render(
    //   <SEO
    //     title={title}
    //     description={description}
    //     image={image}
    //     location={location}
    //     pathname={pathname}
    //   />,
    // );
    // expect(document.title).toBe(title);
    // expect(document.querySelector(`meta[name="description"]`).content).toBe(
    //   description,
    // );
    // expect(document.querySelector(`meta[property="og:title"]`).content).toBe(
    //   title,
    // );
    // expect(
    //   document.querySelector(`meta[property="og:description"]`).content,
    // ).toBe(description);
    // expect(document.querySelector(`meta[property="og:image"]`).content).toBe(
    //   image,
    // );
    // expect(document.querySelector(`meta[name="twitter:title"]`).content).toBe(
    //   title,
    // );
    // expect(
    //   document.querySelector(`meta[name="twitter:description"]`).content,
    // ).toBe(description);
    // expect(document.querySelector(`meta[name="twitter:image"]`).content).toBe(
    //   image,
    // );
    // expect(document.querySelector(`meta[name="twitter:url"]`).content).toBe(
    //   location.href,
    // );
  });
});
