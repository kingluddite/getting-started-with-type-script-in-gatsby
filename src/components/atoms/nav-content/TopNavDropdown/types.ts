export interface TopNavDropdownProps {
  // Define the TopNavDropdownProps interface.
  buttonText: string;
  // The text to display on the dropdown button.
  dropdownLinkData?: any[];
  // The array of links to display in the dropdown menu.
  page?: any;
  // The current page.
  location: Location;
  // The Location object for the current page.
}
